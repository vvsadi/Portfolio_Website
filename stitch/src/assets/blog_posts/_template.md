---
# --- Copy this whole file for each new post; then replace every placeholder below ---
#
# title: The headline readers see on the post page and in the blog list.
title: "Your post title here"
#
# date: Publication date, year-month-day. Used to sort newest-first on the blog index.
date: "YYYY-MM-DD"
#
# slug: The part of the URL for this post (see plan glossary). Lowercase, hyphens, no spaces.
slug: your-post-url-name
#
# excerpt: A short summary (blurb) shown on the blog index under the title—not the full article.
excerpt: "Write one or two sentences that describe what this post is about."
#
# draft: If true, hide from the public list when that feature is implemented. Use false when ready to show.
draft: false
#
# tags: Optional labels to group posts later (each line under tags: starts with "  - ").
tags:
  - topic-one
  - topic-two
---

Write your article **below** the second `---` line. Everything above is settings; everything below is your story.

## Start with a subheading

Use Markdown: lists, [links](https://example.com), `inline code`, and blocks:

```text
Paste code or commands in a fenced block like this.
```

When you are done, save this file with a **new filename** (for example `my-first-post.md`). Do not publish `_template.md` itself—that file stays in the folder as your reusable starter.
